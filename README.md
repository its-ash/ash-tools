# Compressor

This project appears to be a Nuxt.js application with Tailwind CSS, featuring multiple pages and components for various tools such as image, regexp, resume, sandbox, speed, video, webllm, and zip. It also includes Rust/WASM integrations for some tools.

## Features
- **Nuxt.js** frontend with Vue components
- **Tailwind CSS** for styling
- Multiple tool pages: image, regexp, resume, sandbox, speed, video, webllm, zip
- Rust/WASM integrations for performance-critical features (see `docs/*/Cargo.toml` and `public/*/Cargo.toml`)
- PWA support (manifest, service workers)
- Organized assets and documentation

## Project Structure
- `components/` — Vue components (header, footer, etc.)
- `layouts/` — Layout templates
- `pages/` — Main pages for each tool
- `assets/` — CSS and static assets
- `docs/` — Static build output and WASM tool integrations
- `public/` — Public static files and WASM tool integrations

## Getting Started
1. **Install dependencies:**
   ```sh
   pnpm install
   ```
2. **Run the development server:**
   ```sh
   pnpm dev
   ```
3. **Build for production:**
   ```sh
   pnpm build
   ```

## WASM/Rust Tools
Some tools use Rust and WebAssembly. To build these:
- Navigate to the relevant directory (e.g., `docs/image/`, `public/image/`)
- Use `wasm-pack` or `cargo build --target wasm32-unknown-unknown` as appropriate

## License
Specify your license here.

---
*Generated by GitHub Copilot*